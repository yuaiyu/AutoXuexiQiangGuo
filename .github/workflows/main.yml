name: 'action'
on: push
jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'sendDingding'
        run:  ./learn_xi/开始学习.bat
